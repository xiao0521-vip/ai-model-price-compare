# 项目交接文档 —— AI 大模型性价比对比站

> 交接日期：2026-09-19
> 在线地址：https://xuxiao-ai-model.pages.dev
> 仓库：https://github.com/xiao0521-vip/ai-model-price-compare（分支 `main`）
> 本地目录：`D:\AI-Works\Workbuddy\网站开发\model-price-compare`

---

## 一、项目是干什么的

一个**纯静态网页**，对比 76 款主流 AI 大模型的按量计费价格（元/每百万 token），外加 24 组云平台套餐 / Agent 桌面订阅的等效价格对比。无后端、无数据库、无构建步骤——两个文件就是整个网站：`index.html`（页面 + 全部 JS/CSS）和 `data.js`（价格数据）。

特色：每天下午 GitHub Actions 自动抓取 OpenRouter 公开接口的最新价格，改写 `data.js` 后自动提交，Cloudflare Pages 自动重新部署。**全链路无人值守。**

---

## 二、交付文件清单

### 2.1 仓库内文件（12 个，git 追踪中，随仓库自然交付）

| 文件 | 作用 | 二开必读 |
|------|------|----------|
| `index.html` | 整个前端：单页应用，内联 CSS+JS，含三视图切换（按量/云平台套餐/Agent 桌面订阅）、筛选、排序 | ★★★ |
| `data.js` | 全部数据：`var META / MODELS / SUBSCRIPTIONS` 三个顶层变量。**必须用 `var` 声明**（跨 `<script>` 标签访问） | ★★★ |
| `deploy/index.html`、`deploy/data.js` | Cloudflare Pages 的部署目录，**必须与根目录同名文件保持逐字节一致** | ★★★ |
| `scripts/config.js` | 12 个价格数据源配置（OpenRouter 主源 + 11 个 HTML 爬虫源）、汇率 FX 唯一定义处、skip 排除名单 | ★★★ |
| `scripts/lib.js` | 工具库：数据解析/修补（patchDataJs）、单位换算（toYuanPerMillion）、价格判定（judgePrice/SANITY）、模型名匹配（matchModel/coreTokens） | ★★★ |
| `scripts/fetch-prices.js` | 每日抓价主流程：抓取 → 单位闸门 → 唯一命中 → 优惠到期核验 → 写盘 → 语法自检 | ★★★ |
| `scripts/verify-data.js` | 数据体检（CI 每次运行强制执行）：字段完整性、价格区间、异常波动检测、caps 合法性 | ★★ |
| `scripts/sync-caps.js` | 从 OpenRouter 元数据同步「接口能力」（caps 字段）：`--dry-run` 看覆盖率、`--apply` 写盘 | ★★ |
| `.github/workflows/update-prices.yml` | 定时任务：每天 UTC 08:00（北京 16:00）+ 手动触发；每步日志写入 Step Summary | ★★ |
| `package.json` / `package-lock.json` | 依赖声明：axios、cheerio（仅抓价脚本用） | ★ |
| `.gitignore` | 忽略 node_modules、_dev、*.zip 等 | — |

### 2.2 仅在本地的补充交付物（gitignore 中，**需手动拷贝给对方**）

| 文件 | 作用 |
|------|------|
| `_dev/test-sanity.js`（27 断言） | 单位换算/价格判定回归测试 |
| `_dev/test-patch.js`（20 断言） | patchDataJs 只改目标字段、不破坏注释与排版 |
| `_dev/test-match.js`（31 断言） | 模型名匹配（用 OpenRouter 真实 id 做用例） |
| `_dev/test-incident.js`（12 断言） | 事故复现测试：坏数据不得写入 data.js |
| `_dev/check-deployed.js` | 抓线上站点与仓库比对（支持 `PAGES_HOST=xxx` 换域名） |
| `_dev/test-caps.js`（24 断言） | 「接口能力」维度回归：caps 取值合法、HTML 元素齐全、维度/表头/colspan 一致 |

> 强烈建议连同 `_dev/` 一起交付——这 114 条断言是二开时唯一的回归安全网。

### 2.3 ⚠️ 不应外传 / 需要账号交接的内容

**代码文件中没有任何密钥或凭据**（已扫描确认），但以下账号层面的东西需要原主人交出：

| 项目 | 位置 | 说明 |
|------|------|------|
| GitHub 账号/仓库所有权 | github.com | 仓库目前属个人账号 `xiao0521-vip`，交接收器需 Transfer repository 或对方 fork |
| Cloudflare 账号 | dash.cloudflare.com | Pages 项目 `xuxiao-ai-model` 绑定在该账号上，Git 集成需重新授权 |
| GitHub Secrets | 仓库 → Settings → Secrets | **当前为空**，无需交接。若未来配置了 API Key，只在此处可见，切勿写进代码 |
| **不需要** API Key | — | 主力数据源 OpenRouter 是公开接口，免密钥。硅基流动接口已证实不给价格，其 Key **不用配** |

### 2.4 可不交付的本地目录

- `node_modules/`（8.7MB）—— 对方 `npm install` 自行生成
- `.git/`、`_backup-b0741ee/`（历史事故备份，可删）

---

## 三、环境搭建（对方上手 10 分钟）

1. 安装 Node.js ≥ 18（本机用的是 22）
2. 克隆仓库后：`npm install`（只为本地跑抓价脚本；网站本身零依赖）
3. 本地预览网站：直接双击打开 `deploy/index.html`，或 `npx serve deploy`
4. 本地试跑抓价（不写盘）：`node scripts/fetch-prices.js --dry-run`
5. 跑全部回归测试：`for t in test-sanity test-patch test-match test-incident; do node _dev/$t.js; done`
6. 数据体检：`node scripts/verify-data.js`

---

## 四、已完成工作与当前状态

### 4.1 前端功能（全部已上线）

- **按量计费视图**：76 款模型，综合成本 = (输入×3+输出×1)/4 升序排序；厂商/类型/能力/计费/价格来源 5 维筛选；搜索、列排序、重置
- **云平台套餐视图**：8 张卡片，积分/请求数套餐按官方口径展开**模型清单表**（名称/类型/能力/上下文/按量价/来源），附 Credit→token 换算说明
- **Agent 桌面订阅视图**：16 张卡片
- **限时优惠价标注**：模型可带 `promoUntil / regularPm / regularOutPm` 字段，页面展示优惠说明
- **响应式**：1024/768/480px 三断点，手机端可用

### 4.2 自动化抓价链路（全部已实战验证）

```
GitHub Actions（每天北京 16:00 + 可手动触发）
 → Sync to latest main（防检出过期提交）
 → 抓 12 个源（OpenRouter 公开接口为主源，免密钥；另 11 个 HTML 源为备用）
 → 单位闸门（超出 ¥0.01~¥20000/百万 或波动>4倍 → 丢弃 + 告警）
 → 严格匹配（噪声过滤 + 核心词元相等 + 唯一命中，命中≥2 款判歧义丢弃）
 → 优惠到期核验（promoUntil 过期自动恢复常规价）
 → patchDataJs 就地修补（保留注释与排版，绝不整体重写）
 → verify-data 体检（异常直接阻止提交）
 → 有变更才 commit + push（无变更静默跳过）
Cloudflare Pages 监听 main → 自动部署（1-2 分钟生效）
```

**实战记录**：9-18、9-19 两天 bot 均成功自动提交；skip 名单（见下）已验证在生产生效。

### 4.3 数据质量防护（三道防线，都有对应事故背景）

1. **skip 排除名单**（config.js）：OpenRouter 报价与官方不符、经人工核实的模型永久排除，目前含 `gpt-5.6-sol`、`deepseek-v4-flash`、`deepseek-v4-pro`（后两款为用户拍板「以官方价为准」）
2. **单位闸门**（fetch-prices.js）：曾经把 Google 促销价 $1.2014/百万 误乘 10⁶ 写成 ¥852 万，靠体检拦下后根治
3. **数据体检**（verify-data.js）：对比 git 上一版，波动 >50% 警告、>400% 判解析错误直接失败

### 4.4 待完成部分（二开重点）

| 事项 | 现状 | 建议 |
|------|------|------|
| **11 个 HTML 爬虫源未校准** | 全部处于「安全地抓不到」状态（白名单挡住噪声，不会写错，但产出 0） | 逐个对照真实页面调 `scripts/config.js` 里 `tableSource()` 的 only 白名单；用 `node scripts/fetch-prices.js --only=源名 --dry-run` 单源调试 |
| OpenAI 源 403 | 反爬 | 需换 UA/代理或放弃 |
| 优惠价促销信息靠人工录入 | 机制已建好（每日自动核验到期），但新促销需人工发现 | 可加爬取促销公告的源 |
| 前端回归测试缺失 | `_dev/` 只有数据层测试；index.html 的 DOM mock 测试曾存在但被误删 | 若要大改前端，建议先重建 DOM mock 测试 |
| 硬编码 | 汇率 7.1（config.js 一处，体检校验与 META.fx 一致） | 汇率波动大时可改为拉取实时汇率 |

---

## 五、GitHub 状态盘点

- **分支**：只有 `main`（远程）/ `master`（本地），无其他分支
- **Pull Request**：**无**（早期 PR 方案已废弃，现为直接 push main）
- **Issue**：无　**Release/Tag**：无
- **提交历史**：共 19 个提交，两类作者——人工提交（`xiao0521-vip`）与 `price-bot`（自动化，GitHub Actions 用仓库自带的 `github-actions[bot]` 身份）
- **关键提交节点**：

```
75a6643  chore(prices): auto-update 2026-09-19      ← bot 16:09 首次每日定时运行 ✅
94067c1  Merge（本地 cron 改每天 + bot 合并点）
1319915  ci: 定时抓价改为每天
b6195bd  fix(data): DeepSeek 锁定官方价 + skip 名单加固
91d848f  refactor(pipeline): 全面审查修复 12 处缺陷 + 76 款模型 + 优惠价机制
2d3e3ce  feat(source): 接入 OpenRouter —— 站点首次真正自动更新
0d7595b  fix(data): GPT-5.6 Sol / Gemini 3.6 Flash 人工核实修正
0011125  fix(parser): 修复价格单位换算（×10⁶ 事故）
22f6984  fix(ci): 废弃 PR 方案改为直推 main + 新增体检
638fb1d  init: price comparison site + auto-update scripts  ← 起点
```

- **Secrets**：空（无任何密钥）
- **Actions 权限**：Workflow permissions 已设为 Read and write（自动化 push 的前提，勿改回）

---

## 六、日常运维手册（给接手人）

### 6.1 改数据/改页面的标准流程

```powershell
# 本地改完 index.html / data.js 后，必须同步 deploy/ 目录（两个文件都要）
Copy-Item index.html deploy/index.html
Copy-Item data.js deploy/data.js
# 提交推送
git add -A
git commit -m "说明改了什么"
git push -u origin master:main    # 注意本地叫 master、远程叫 main
```

### 6.2 推送被拒（`fetch first` / `non-fast-forward`）

正常现象——bot 抢先提交了新价格：

```powershell
git pull origin main --no-rebase   # 必须用 --no-rebase，不要 rebase
git push -u origin master:main
```

### 6.3 手动触发抓价

仓库 → Actions → Daily Price Update → Run workflow。失败先看 **Summary 标签页**（所有步骤日志都在那里，不用展开单步）。

### 6.4 改价格口径（如「某款以官方为准」）

1. `data.js` 改该模型的 `inputPm/outputPm`（记得同步 `deploy/data.js`）
2. `scripts/config.js` 的 OpenRouter `skip` 名单加入对应 id（核心词元匹配，变体后缀自动覆盖）
3. 推送

### 6.5 日志中的告警怎么看

- `::warning title=解析器口径待校准::` → 某源产出了被闸门丢弃的可疑值，需人工核对该源
- 「未匹配到总表条目」报告 → 源返回了新模型名，可能需要补进总表
- 「多源冲突」报告 → 同一模型多源给出不同价，已按源优先级取值

---

## 七、踩坑记录（重要，前人血泪）

1. `data.js` 顶层必须 `var` 声明（`const` 不挂 window，页面会白屏）
2. **绝不整体重写 MODELS**——只能用 `patchDataJs` 就地修补，否则丢失全部注释和排版
3. 价格单位唯一出口：**元/每百万 token**。HTML 源解析用 `quotePer` 声明页面报价单位；没有币种标记的数字（如「3,500万 Tokens」配额）一律不当作价格
4. 模型名匹配宁可漏更新不可配错：唯一命中规则 + 噪声过滤，禁止双向 `includes`（曾让碎片名 `"2.0"` 命中真模型写入错价）
5. 本地分支 `master` → 远程 `main`，推送命令固定为 `git push -u origin master:main`
6. **合并远程只用 `git pull --no-rebase` 或 `git merge`，绝不要 rebase**（曾因 rebase 被中断导致 .git 损坏，靠备份+重建 refs 才恢复）
7. `*.pages.dev` 子域在**创建项目时**定死，Cloudflare 改项目名不会换域名
8. Cloudflare 新建项目时默认落在 Workers 流程（出现 `npx wrangler deploy` 就是走错了），必须切 **Pages** 标签，且 Build output directory 填 `deploy/`
9. CI 的 `workflow_dispatch` 会固定检出「派发那一刻」的提交，所以 workflow 里有 Sync to latest main 步骤，勿删

---

## 八、给接手人的第一周建议

1. Day 1：按第三节搭环境 → 跑一遍 90 条测试和 dry-run → 打开线上站点对照
2. Day 2-3：读 `scripts/lib.js`（换算与匹配是全项目正确性的核心），跑 `_dev/test-match.js` 看用例理解规则
3. Day 4 起：挑 1 个 HTML 源（推荐 DeepSeek 官网，静态表格最简单）做首个校准练手
4. 随时：改动后必跑 `verify-data.js` + 4 个测试文件，全绿再推
